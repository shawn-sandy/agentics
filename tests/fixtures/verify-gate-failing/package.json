{
  "name": "verify-gate-failing",
  "private": true,
  "version": "0.0.0",
  "description": "Fixture: the unit stage exits 1 with no dependencies to install.",
  "scripts": {
    "test": "exit 1"
  }
}
