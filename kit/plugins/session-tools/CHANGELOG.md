# Changelog

## 0.1.0 — 2026-07-02

- Initial release with the `export-session` skill: converts a Claude Code session JSONL transcript into a Markdown file under `{plansDirectory}/sessions/` (frontmatter + user/Claude turns, harness noise filtered out) via a bundled Python script.
