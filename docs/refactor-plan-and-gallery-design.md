# Rebuild the plan document and gallery design

> Generated plan pages are hard to scan — two stacked summaries, every section in an identical bordered box, the Verify line hidden behind a disclosure, three...

<!-- generated:start -->

**Status:** Shipped 2026-08-17  **Plan:** [refactor-plan-and-gallery-design.md](plans/refactor-plan-and-gallery-design.md)
**Type:** refactor

## What shipped

- Replace the `:root` token block in `plan-shell.mjs`'s `CSS` export with the prototype's palette and font stacks — `--...
- Narrow the back-compat guard at `tests/plugins/test-build-plan-html.mjs:883` from a whole-document `assert.equal(stri...
- Add the theme toggle — a `data-theme`-reading inline script at the top of `<head>` in `page()`, a labelled toggle but...
- Nest the At-a-glance block inside the objective card — change `objectiveCard(objective)` to `objectiveCard(objective,...
- Add the step rail to the sidebar — give each `.step-card` an `id="step-N"` while keeping `class="step-card completed"...
- Fix the scroll-spy so it clears the active link when nothing intersects — the observer at `plan-shell.mjs:1534-1545`...
- Re-copy `scripts/build-plan-html.mjs` and `scripts/lib/plan-shell.mjs` over their counterparts under `kit/plugins/pla...
- Rebuild the controls in `kit/plugins/plan-agent/templates/plans-gallery.html` — replace the three filter-chip rows wi...
- Emit `data-month` on each card and sort in-progress plans ahead of the date order in all three byte-identical copies...
- Add `tests/plugins/test-plan-redesign.mjs` asserting the objective end to end.

## Files changed

| Path | Role | Status |
| ---- | ---- | ------ |
| `scripts/lib/plan-shell.mjs` | tokens, dark palette, type roles, theme toggle, objective... | Modified |
| `scripts/build-plan-html.mjs` | nest the glance in the objective card and pass the step l... | Modified |
| `kit/plugins/plan-agent/scripts/lib/plan-shell.mjs` | re-copied from the repo-root source | Modified |
| `kit/plugins/plan-agent/scripts/build-plan-html.mjs` | re-copied from the repo-root source | Modified |
| `kit/plugins/plan-agent/templates/plans-gallery.html` | search plus status segmented control, type and effort dis... | Modified |
| `kit/plugins/plan-agent/hooks/build-index.sh` | `data-month` attribute and in-progress-first sort | Modified |
| `scripts/build-plans-index.sh` | same edit, kept byte-identical | Modified |
| `docs/plans/build-index.sh` | same edit, kept byte-identical | Modified |
| `docs/plans/index.html` | regenerated by the build script | Modified |
| `docs/artifacts/index.html` | regenerated by the build script | Modified |
| `docs/prototypes/index.html` | regenerated by the build script | Modified |
| `tests/plugins/test-build-plan-html.mjs` | narrow the back-compat guard; the two nav id arrays stay ... | Modified |
| `tests/plugins/test-plan-redesign.mjs` | objective-verification smoke test | Created |
| `.claude-plugin/marketplace.json` | plan-agent 7.4.4 to 7.5.0 | Modified |
| `kit/plugins/plan-agent/CHANGELOG.md` | 7.5.0 entry | Modified |

## How it works

Rebuild the presentation layer of generated plan pages and the gallery index — design tokens with a persisted dark theme, a mono-and-serif type system, one merged goal panel, an always-visible Verify line, a sidebar step rail that absorbs the progress bar, and gallery controls that surface in-flight work first — without changing the DOM contract that `extractSections` and the gallery generator rea.

Every generated plan is styled by one 1930-line module, `scripts/lib/plan-shell.mjs`, whose `CSS` export has drifted into a generic default: `system-ui` at 15px, `#2563eb` blue, `4px` radius, and a `1px solid #e5e7eb` border on every container. A visual audit of a rendered plan, the 88-card gallery at `docs/plans/index.html`, and the hub at `docs/index.html` found six concrete failures, and two prototypes at `docs/prototypes/plan-document-redesign.html` and `docs/prototypes/plans-site-redesign.h...

The implementation proceeded through these steps: Replace the `:root` token block in `plan-shell.mjs`'s `CSS` export with the prototype's palette and font stacks — `--...; Narrow the back-compat guard at `tests/plugins/test-build-plan-html.mjs:883` from a whole-document `assert.equal(stri...; Add the theme toggle — a `data-theme`-reading inline script at the top of `<head>` in `page()`, a labelled toggle but...; Nest the At-a-glance block inside the objective card — change `objectiveCard(objective)` to `objectiveCard(objective,...; Add the step rail to the sidebar — give each `.step-card` an `id="step-N"` while keeping `class="step-card completed"....

## Commit history

| SHA | Date | Subject |
| --- | ---- | ------- |
| `f6b0bdd` | 2026-08-17 | docs(plans): mark settings-sync guard next-step done in add-verification-gates ( |

<!-- generated:end -->

## References

- Plan: [refactor-plan-and-gallery-design.md](plans/refactor-plan-and-gallery-design.md)
- Issue: https://github.com/shawn-sandy/agentics/issues/500
